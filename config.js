var config = {
    token: '',
    channel: '' // the channel id
}

// TOKEN: Bot token
// https://discordapp.com/developers/applications/me 
// Create an APP
// Once created, generate a token
// Paste token in above quotes


// Channel: Channel ID (Right click with developer mode enabled)

module.exports = config;